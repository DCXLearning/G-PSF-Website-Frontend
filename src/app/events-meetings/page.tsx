import EventsListPage from "@/components/News&Updates/list-New&Update/Seemore_Events";

export default function EventsMeetingsPage() {
    // Reuse the existing events list UI for the new /events-meetings route.
    return <EventsListPage />;
}
